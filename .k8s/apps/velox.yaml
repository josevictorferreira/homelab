apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kubenix/k8s-version: "1.32"
    kubenix/project-name: ze-homelab
  labels:
    app.kubernetes.io/controller: main
    app.kubernetes.io/instance: velox
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: velox
    helm.sh/chart: app-template-4.2.0
    kubenix/hash: 9c253f4a00516b07ba440f9f57e5167913253c13
    kubenix/module-instance: velox
    kubenix/module-name: release
    kubenix/module-version: 1.0.0
  name: velox
  namespace: apps
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/controller: main
      app.kubernetes.io/instance: velox
      app.kubernetes.io/name: velox
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        velox.josevictor.me/config-hash: 95de210aff7949660ceee842153db3ec623319ce0640334e181f6d89651f4eb9
        velox.josevictor.me/oauth-agent-config-hash: 72b7f1b202300c363471312cd880f37085429ad305f9944b37c6926c9dee941d
      labels:
        app.kubernetes.io/controller: main
        app.kubernetes.io/instance: velox
        app.kubernetes.io/name: velox
    spec:
      automountServiceAccountToken: false
      containers:
        - args:
            - --config
            - /etc/velox/velox.toml
          envFrom:
            - secretRef:
                name: velox-config
          image: ghcr.io/josevictorferreira/velox:latest
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          name: main
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 5
          resources:
            limits:
              cpu: 1000m
              memory: 256Mi
            requests:
              cpu: 50m
              memory: 64Mi
          volumeMounts:
            - mountPath: /etc/velox/velox.toml
              name: config
              readOnly: true
              subPath: velox.toml
            - mountPath: /etc/velox/oauth-runtime
              name: oauth-runtime
              readOnly: true
      dnsPolicy: ClusterFirst
      enableServiceLinks: false
      hostIPC: false
      hostNetwork: false
      hostPID: false
      imagePullSecrets:
        - name: ghcr-registry-secret
      initContainers:
        - env:
            - name: ANTIGRAVITY_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  key: ANTIGRAVITY_CLIENT_SECRET
                  name: velox-config
          image: ghcr.io/josevictorferreira/velox-oauth-agent:latest
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 15
          name: velox-oauth-agent
          resources:
            limits:
              cpu: 500m
              memory: 128Mi
            requests:
              cpu: 50m
              memory: 64Mi
          restartPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsGroup: 65532
            runAsNonRoot: true
            runAsUser: 65532
          volumeMounts:
            - mountPath: /etc/velox-oauth/agent.toml
              name: agent-config
              readOnly: true
              subPath: agent.toml
            - mountPath: /var/run/secrets/kubernetes.io/serviceaccount
              name: agent-sa-token
              readOnly: true
            - mountPath: /oauth-runtime
              name: oauth-runtime
        - args:
            - |
              for _ in $(seq 1 60); do
                if [ -f /etc/velox/oauth-runtime/codex-primary.access-token ] \
                  && [ -f /etc/velox/oauth-runtime/antigravity-primary.access-token ] \
                  && [ -f /etc/velox/oauth-runtime/antigravity-secondary.access-token ]; then
                  echo "oauth credential files published"
                  exit 0
                fi
                sleep 1
              done
              echo "timed out waiting for oauth credential files; starting Velox anyway" >&2
              exit 0
          command:
            - sh
            - -c
          image: busybox:1.36.1
          name: wait-for-oauth-tokens
          resources:
            limits:
              cpu: 100m
              memory: 64Mi
            requests:
              cpu: 50m
              memory: 64Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsGroup: 65532
            runAsNonRoot: true
            runAsUser: 65532
          volumeMounts:
            - mountPath: /etc/velox/oauth-runtime
              name: oauth-runtime
              readOnly: true
      serviceAccountName: velox-oauth-agent
      terminationGracePeriodSeconds: 45
      volumes:
        - configMap:
            items:
              - key: agent.toml
                path: agent.toml
            name: velox-oauth-agent-config
          name: agent-config
        - name: agent-sa-token
          projected:
            defaultMode: 420
            sources:
              - serviceAccountToken:
                  expirationSeconds: 3607
                  path: token
              - configMap:
                  items:
                    - key: ca.crt
                      path: ca.crt
                  name: kube-root-ca.crt
        - configMap:
            items:
              - key: velox.toml
                path: velox.toml
            name: velox-config
          name: config
        - emptyDir: {}
          name: oauth-runtime
---
apiVersion: v1
kind: Service
metadata:
  annotations:
    kubenix/k8s-version: "1.32"
    kubenix/project-name: ze-homelab
    lbipam.cilium.io/ips: 10.10.10.158
    lbipam.cilium.io/sharing-key: velox
  labels:
    app.kubernetes.io/instance: velox
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: velox
    app.kubernetes.io/service: velox
    helm.sh/chart: app-template-4.2.0
    kubenix/hash: 9c253f4a00516b07ba440f9f57e5167913253c13
    kubenix/module-instance: velox
    kubenix/module-name: release
    kubenix/module-version: 1.0.0
  name: velox
  namespace: apps
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app.kubernetes.io/controller: main
    app.kubernetes.io/instance: velox
    app.kubernetes.io/name: velox
  type: LoadBalancer
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubenix/k8s-version: "1.32"
    kubenix/project-name: ze-homelab
  labels:
    app.kubernetes.io/instance: velox
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: velox
    helm.sh/chart: app-template-4.2.0
    kubenix/hash: 9c253f4a00516b07ba440f9f57e5167913253c13
    kubenix/module-instance: velox
    kubenix/module-name: release
    kubenix/module-version: 1.0.0
  name: velox
  namespace: apps
spec:
  ingressClassName: cilium
  rules:
    - host: velox.josevictor.me
      http:
        paths:
          - backend:
              service:
                name: velox
                port:
                  number: 8080
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - velox.josevictor.me
      secretName: wildcard-tls
---
apiVersion: v1
data:
  velox.toml: |
    # Velox configuration ported from OmniRoute/9router.
    #
    # Scope: only providers that authenticate with a static API key and speak either
    # the OpenAI chat-completions or the Anthropic Messages protocol. OAuth
    # subscription providers (Claude Pro, ChatGPT/Codex, Antigravity, Gemini CLI,
    # Kiro, Kimi-coding OAuth) and bespoke protocols (CommandCode, Vertex, Ollama)
    # are not represented; combos that depended solely on them are absent rather
    # than present-but-degraded.
    #
    # Endpoints come from 9router's open-sse/config/providers.js. Velox's `base_url`
    # names the API root, so the `/chat/completions` and `/messages` suffixes that
    # appear there are stripped here.

    [server]
    bind = "0.0.0.0:8080"
    api_keys_env = "VELOX_API_KEYS"
    log_level = "info"
    log_format = "json"

    [limits]
    max_body_bytes = 4194304
    shutdown_drain_ms = 10000
    shutdown_grace_ms = 30000
    # Default TTFB (30s) kills LLM extraction calls that carry a whole story in
    # the prompt: GLM/opencode-go routinely take 30-60s before the first byte,
    # so both combo targets time out and the request dies as 502 at ~60s.
    # There is no total-request timeout in velox; stream_idle_timeout_ms guards
    # a stalled stream after the head arrives. Reasoning models (e.g. gpt-5.6-luna
    # on gandalf) can spend 60-120s thinking on large contexts.
    time_to_first_byte_timeout_ms = 120000
    stream_idle_timeout_ms = 180000
    # `models.<alias>.max_tokens` only fills in for a client that omits an output
    # ceiling, which pi never does: it sends 16384 on every agent turn whatever the
    # model, GLM spends that inside its own thinking block, and the answer comes
    # back truncated with `finish_reason: "length"`. This raises such a ceiling to
    # the target's declared `max_tokens`, but only on requests carrying `tools` --
    # the one shape where a small ceiling is never a deliberate "keep it short" --
    # and only for targets that declare one, so it can never exceed what an upstream
    # accepts. Targets without a declared ceiling are left exactly as the client
    # sent them. (OmniRoute did the same thing implicitly, floor 32000.)
    min_client_max_tokens = 131072

    # Apply both input compressors globally to every configured combo.
    [compression.rtk]
    enabled = true

    [compression.caveman]
    enabled = true

    # ---------------------------------------------------------------------------
    # Providers
    # ---------------------------------------------------------------------------

    # OpenCode Zen "go" plan. Serves most models over OpenAI chat-completions.
    [providers.opencode-go]
    kind = "openai-compatible"
    base_url = "https://opencode.ai/zen/go/v1"
    api_key_env = "OPENCODE_GO_API_KEY"

    # The same host also exposes /messages, which 9router uses for the MiniMax
    # models specifically. Velox binds one wire format per provider, so the
    # Anthropic-speaking half is declared separately against the same credential.
    [providers.opencode-go-anthropic]
    kind = "anthropic"
    base_url = "https://opencode.ai/zen/go/v1"
    api_key_env = "OPENCODE_GO_API_KEY"

    [providers.openrouter]
    kind = "openai-compatible"
    base_url = "https://openrouter.ai/api/v1"
    api_key_env = "OPENROUTER_API_KEY"

    # Providers that accept image content parts. OpenRouter, Mistral, and the
    # Google Gemini models exposed through Antigravity are marked vision-capable;
    # vision requests are filtered against these provider capabilities.
    [providers.openrouter.capabilities]
    vision = true

    # Z.AI / GLM. API key, but the endpoint is Anthropic-shaped.
    [providers.glm]
    kind = "anthropic"
    base_url = "https://api.z.ai/api/anthropic/v1"
    api_key_env = "GLM_API_KEY"

    # Alibaba Model Studio token plan (OmniRoute: openai-compatible-chat-dbe58ac9).
    [providers.alibaba-token-plan]
    kind = "openai-compatible"
    base_url = "https://token-plan.ap-southeast-1.maas.aliyuncs.com/compatible-mode/v1"
    api_key_env = "ALIBABA_TOKEN_PLAN_API_KEY"

    # OmniRoute: openai-compatible-chat-4eea86d8. pippin's primary target.
    [providers.yoloauto]
    kind = "openai-compatible"
    base_url = "https://yolo-auto.com/v1"
    api_key_env = "YOLOAUTO_API_KEY"

    # OmniRoute: openai-compatible-chat-06a1719a.
    [providers.neuralwatt]
    kind = "openai-compatible"
    base_url = "https://api.neuralwatt.com/v1"
    api_key_env = "NEURALWATT_API_KEY"

    [providers.xiaomi-mimo]
    kind = "openai-compatible"
    base_url = "https://token-plan-sgp.xiaomimimo.com/v1"
    api_key_env = "XIAOMI_MIMO_API_KEY"

    [providers.kimi-coding]
    kind = "anthropic"
    base_url = "https://api.kimi.com/coding"
    api_key_env = "KIMI_API_KEY"

    [providers.cerebras]
    kind = "openai-compatible"
    base_url = "https://api.cerebras.ai/v1"
    api_key_env = "CEREBRAS_API_KEY"

    [providers.nvidia]
    kind = "openai-compatible"
    base_url = "https://integrate.api.nvidia.com/v1"
    api_key_env = "NVIDIA_API_KEY"

    [providers.mistral]
    kind = "openai-compatible"
    base_url = "https://api.mistral.ai/v1"
    api_key_env = "MISTRAL_API_KEY"

    # The second provider that accepts image content parts, verified the same way as
    # openrouter: sent a data: URL image and it named the colour correctly. It exists
    # so image work survives an openrouter outage rather than stopping dead.
    [providers.mistral.capabilities]
    vision = true

    [providers.bazaarlink]
    kind = "openai-compatible"
    base_url = "https://bazaarlink.ai/api/v1"
    api_key_env = "BAZAARLINK_API_KEY"

    [providers.puter]
    kind = "openai-compatible"
    base_url = "https://api.puter.com/puterai/openai/v1"
    api_key_env = "PUTER_API_KEY"

    # PGS Grove. Its /v1 surface is OpenAI-shaped, so the stock
    # `openai-compatible` adapter serves it unchanged -- verified end to end
    # against a local velox: /models, a non-streaming completion, an SSE stream,
    # and a tool call all came back correct.
    #
    # GET /v1/models advertises 38 models, but that is the vendor catalog rather
    # than an entitlement list: everything outside the plan answers 400
    # `model_not_in_plan`, and only the three chat models aliased below actually
    # serve. Because that refusal is a 400, an unentitled alias in a combo would
    # fail the whole attempt rather than fall through -- so only verified-serving
    # models are aliased here.
    #
    # `embeddinggemma-300m` and `kokoro-82m` are listed and reachable on this host
    # but are billed from prepaid API credits the plan does not include (400
    # `endpoint_not_in_plan`), so no `embedding`/`tts` capability is declared and
    # the default openai-compatible capability set (streaming + tools, no vision)
    # is exactly right: both entitled families refuse image parts with 400
    # `unsupported_content`.
    #
    # The plan's own consumption is exposed at GET /v1/usage
    # (weekly/daily used-percent). Velox has no quota adapter for this provider
    # yet; see .agents/specs/0002-provider-quotas in the velox repo.
    [providers.pgs-grove]
    kind = "openai-compatible"
    base_url = "https://api.pgsgrove.com/v1"
    api_key_env = "PGS_GROVE_API_KEY"

    [providers.nanogpt]
    kind = "openai-compatible"
    base_url = "https://nano-gpt.com/api/v1"
    api_key_env = "NANOGPT_API_KEY"

    # NanoGPT's image surface lives under a different prefix than its chat API.
    # Serves /images/generations (incl. imageDataUrl edits) for Domus.
    [providers.nanogpt-images]
    kind = "openai-compatible"
    base_url = "https://nano-gpt.com/v1"
    api_key_env = "NANOGPT_API_KEY"

    [providers.nanogpt-images.capabilities]
    streaming = false
    tools = false
    image_generation = true

    # OpenRouter's image surface. Same host and same key as [providers.openrouter],
    # declared as its own instance so the image-only capability set stays accurate
    # — exactly how openrouter-audio is split out for STT.
    #
    # The OpenAI-shaped /images/generations path is an alias for OpenRouter's own
    # /images API, so no new provider kind is needed; responses always come back as
    # data[].b64_json (+ media_type), and `response_format` is accepted but ignored.
    #
    # Edits work here, but NOT through the NanoGPT fields: OpenRouter takes
    # reference images in `input_references` and silently ignores `imageDataUrl`,
    # answering an edit request with an unrelated fresh image. That is why no
    # openrouter model shares a combo with `hidream` — see [combos.image] below.
    [providers.openrouter-images]
    kind = "openai-compatible"
    base_url = "https://openrouter.ai/api/v1"
    api_key_env = "OPENROUTER_API_KEY"

    [providers.openrouter-images.capabilities]
    streaming = false
    tools = false
    image_generation = true

    # Voyage AI, embeddings only. Its /embeddings surface is OpenAI-shaped, which is
    # the prerequisite for the `embedding` capability; it serves no chat models, so
    # streaming and tools are declared off and no combo targets it.
    [providers.voyage]
    kind = "openai-compatible"
    base_url = "https://api.voyageai.com/v1"
    api_key_env = "VOYAGE_API_KEY"

    [providers.voyage.capabilities]
    streaming = false
    tools = false
    embedding = true

    # --- Audio (TTS) ------------------------------------------------------------
    # Speechify TTS (11-class voice model). `voice` is supplied per request; when
    # the client omits it the adapter applies its curated default voice. Speechify
    # is TTS-only: do not set `stt = true` here or validation fails at startup.
    [providers.speechify]
    kind = "speechify"
    base_url = "https://api.speechify.ai"
    api_key_env = "SPEECHIFY_API_KEY"

    [providers.speechify.capabilities]
    tts = true

    # --- Audio (STT) -----------------------------------------------------------
    # OpenRouter STT (bespoke `openrouter` kind: JSON body with base64 audio, not
    # multipart). Distinct provider instance from the chat `[providers.openrouter]`
    # so its audio adapter and `stt` capability stay isolated. Reuses the same
    # OPENROUTER_API_KEY secret. Verified against openai/whisper-1 (0.0002 cr/min).
    [providers.openrouter-audio]
    kind = "openrouter"
    base_url = "https://openrouter.ai/api/v1"
    api_key_env = "OPENROUTER_API_KEY"

    [providers.openrouter-audio.capabilities]
    stt = true

    # ---------------------------------------------------------------------------
    # OAuth subscription providers
    #
    # These are served by the velox-oauth-agent sidecar, which owns the OAuth
    # flow and refresh and publishes access-token + metadata files to the shared
    # oauth-runtime emptyDir. Velox reads the files at dispatch time, so rotation
    # needs no restart. The Antigravity entries below use the explicitly approved
    # OmniRoute client registration and the two migrated account records.
    # Full procedure: docs/oauth-operator-runbook.md in the velox repo.
    #
    # [providers.claude-oauth]
    # kind = "anthropic"
    # base_url = "https://api.anthropic.com/v1"
    # credential_file = "/etc/velox/oauth-runtime/claude-primary.access-token"
    #
    [providers.codex-subscription]
    kind = "codex"
    base_url = "https://chatgpt.com/backend-api/codex/responses"
    credential_file = "/etc/velox/oauth-runtime/codex-primary.access-token"
    codex_metadata_file = "/etc/velox/oauth-runtime/codex-primary.metadata.json"

    [providers.codex-subscription.capabilities]
    streaming = true
    tools = true
    vision = false
    structured_output = false
    embedding = false

    [providers.antigravity-primary]
    kind = "antigravity"
    base_url = "https://daily-cloudcode-pa.googleapis.com"
    credential_file = "/etc/velox/oauth-runtime/antigravity-primary.access-token"
    antigravity_metadata_file = "/etc/velox/oauth-runtime/antigravity-primary.metadata.json"

    [providers.antigravity-primary.capabilities]
    streaming = true
    tools = true
    vision = true
    structured_output = false

    [providers.antigravity-secondary]
    kind = "antigravity"
    base_url = "https://daily-cloudcode-pa.googleapis.com"
    credential_file = "/etc/velox/oauth-runtime/antigravity-secondary.access-token"
    antigravity_metadata_file = "/etc/velox/oauth-runtime/antigravity-secondary.metadata.json"

    [providers.antigravity-secondary.capabilities]
    streaming = true
    tools = true
    vision = true
    structured_output = false

    # ---------------------------------------------------------------------------
    # Models
    #
    # One alias per (provider, upstream model) pair a combo needs. Aliases are
    # prefixed with the provider so the same upstream model reached through two
    # providers stays addressable.
    #
    # `max_tokens` appears only on aliases whose provider is `kind = "anthropic"`.
    # The Messages API makes the field mandatory, so Velox has to send a value for
    # the many clients that omit it (pi omits it on every agent turn), and its
    # built-in fallback is a conservative 16384 — low enough that long answers came
    # back truncated with `finish_reason: "length"`. The values below are the
    # ceilings each upstream actually accepts: z.ai reports the range as [1,131072]
    # and rejects 200000, and opencode-go's /messages surface took 131072 alongside
    # an 80k-token prompt. Both treat it as a cap rather than a reserved budget, so
    # a large prompt does not push the request over the context window.
    #
    # `kimi-k3-kimi-coding` is deliberately left without one: that endpoint is
    # currently answering 404, so no ceiling could be verified against it, and the
    # adapter default applies until it can be.
    # ---------------------------------------------------------------------------
    # TTS model alias: resolves `model="tts-1"` to Speechify TTS. The upstream
    # model id is sent as Speechify `model`; the voice comes from the per-request
    # `voice` field (adapter curated default if omitted).
    [models.tts-1]
    provider = "speechify"
    upstream_model = "simba-english"
    # STT model alias: resolves `model="stt-1"` to OpenRouter Whisper. Multipart
    # uploads (file + model); the OpenRouter adapter sends base64 audio in JSON.
    [models.stt-1]
    provider = "openrouter-audio"
    upstream_model = "openai/whisper-1"

    [models.glm-5-3-zai]
    provider = "glm"
    upstream_model = "glm-5.3"
    max_tokens = 131072

    [models.glm-5-3-flash-zai]
    provider = "glm"
    upstream_model = "glm-5.3-flash"
    max_tokens = 131072

    [models.glm-5-2-zai]
    provider = "glm"
    upstream_model = "glm-5.2"
    max_tokens = 131072

    [models.glm-5-2-max-zai]
    provider = "glm"
    upstream_model = "glm-5.2-max"
    max_tokens = 131072

    [models.glm-5-1-zai]
    provider = "glm"
    upstream_model = "glm-5.1"
    max_tokens = 131072

    [models.glm-4-5-air-zai]
    provider = "glm"
    upstream_model = "glm-4.5-air"
    max_tokens = 131072

    [models.glm-5-3-ocgo]
    provider = "opencode-go"
    upstream_model = "glm-5.3"

    [models.glm-5-3-flash-ocgo]
    provider = "opencode-go"
    upstream_model = "glm-5.3-flash"

    [models.glm-5-3-flash-pgsgrove]
    provider = "pgs-grove"
    upstream_model = "glm-5.3-flash"

    [models.glm-5-2-ocgo]
    provider = "opencode-go"
    upstream_model = "glm-5.2"

    [models.glm-5-1-ocgo]
    provider = "opencode-go"
    upstream_model = "glm-5.1"

    [models.glm-5-2-alibaba]
    provider = "alibaba-token-plan"
    upstream_model = "glm-5.2"

    [models.glm-5-2-short-neuralwatt]
    provider = "neuralwatt"
    upstream_model = "glm-5.2-short"

    [models.glm-5-2-nanogpt]
    provider = "nanogpt"
    upstream_model = "glm-5.2"

    # Image edit model used by Domus (DOMUS_VELOX_IMAGE_MODEL).
    # Use NanoGPT's current HiDream O1 edit-capable model, not the legacy
    # `hidream` generation alias, which can redraw the source room.
    [models.hidream]
    provider = "nanogpt-images"
    upstream_model = "hidream-o1-image"

    # OpenRouter image models. Every alias below was verified end to end against
    # /images/generations (200 + a decoded image); the two `-preview` slugs
    # OpenRouter also lists are the same endpoints and are deliberately omitted.
    # These models reject `n > 1`; ask for one image per request.
    [models.gemini-3-1-flash-image-openrouter]
    provider = "openrouter-images"
    upstream_model = "google/gemini-3.1-flash-image"

    [models.gemini-3-1-flash-lite-image-openrouter]
    provider = "openrouter-images"
    upstream_model = "google/gemini-3.1-flash-lite-image"

    [models.gemini-3-pro-image-openrouter]
    provider = "openrouter-images"
    upstream_model = "google/gemini-3-pro-image"

    [models.gemini-2-5-flash-image-openrouter]
    provider = "openrouter-images"
    upstream_model = "google/gemini-2.5-flash-image"

    [models.gpt-5-image-openrouter]
    provider = "openrouter-images"
    upstream_model = "openai/gpt-5-image"

    [models.gpt-5-image-mini-openrouter]
    provider = "openrouter-images"
    upstream_model = "openai/gpt-5-image-mini"

    [models.gpt-5-4-image-2-openrouter]
    provider = "openrouter-images"
    upstream_model = "openai/gpt-5.4-image-2"

    [models.qwen3-6-35b-yoloauto]
    provider = "yoloauto"
    upstream_model = "qwen3.6-35b-a3b"

    [models.qwen3-7-plus-alibaba]
    provider = "alibaba-token-plan"
    upstream_model = "qwen3.7-plus"

    [models.qwen3-7-max-alibaba]
    provider = "alibaba-token-plan"
    upstream_model = "qwen3.7-max"

    [models.qwen3-7-max-ocgo]
    provider = "opencode-go"
    upstream_model = "qwen3.7-max"

    [models.qwen3-8-max-alibaba]
    provider = "alibaba-token-plan"
    upstream_model = "alibaba-token-plan/qwen3.8-max"

    [models.qwen3-8-max-openrouter]
    provider = "openrouter"
    upstream_model = "qwen/qwen3.8-max"

    [models.qwen3-6-plus-alibaba]
    provider = "alibaba-token-plan"
    upstream_model = "qwen3.6-plus"

    [models.qwen3-6-plus-ocgo]
    provider = "opencode-go"
    upstream_model = "qwen3.6-plus"

    [models.deepseek-v4-flash-openrouter]
    provider = "openrouter"
    upstream_model = "deepseek/deepseek-v4-flash-0731"

    [models.deepseek-v4-flash-alibaba]
    provider = "alibaba-token-plan"
    upstream_model = "alibaba-token-plan/deepseek-v4-flash-0731"

    [models.deepseek-v4-flash-neuralwatt]
    provider = "neuralwatt"
    upstream_model = "deepseek-v4-flash"

    # pgs-grove entitles the dated 0731 snapshot, not the bare `deepseek-v4-flash`
    # slug, which answers `model_not_in_plan`. `-turbo` is a second route to the
    # same snapshot; measured latency was indistinguishable, so it earns its place
    # as a same-provider fallback rather than as a faster primary.
    [models.deepseek-v4-flash-pgsgrove]
    provider = "pgs-grove"
    upstream_model = "deepseek-v4-flash-0731"

    [models.deepseek-v4-flash-turbo-pgsgrove]
    provider = "pgs-grove"
    upstream_model = "deepseek-v4-flash-0731-turbo"

    [models.deepseek-v4-pro-ocgo]
    provider = "opencode-go"
    upstream_model = "deepseek-v4-pro"

    [models.kimi-k2-5-alibaba]
    provider = "alibaba-token-plan"
    upstream_model = "kimi-k2.5"

    [models.kimi-k2-5-ocgo]
    provider = "opencode-go"
    upstream_model = "kimi-k2.5"

    [models.kimi-k2-6-ocgo]
    provider = "opencode-go"
    upstream_model = "kimi-k2.6"

    [models.kimi-k2-7-code-ocgo]
    provider = "opencode-go"
    upstream_model = "kimi-k2.7-code"

    [models.kimi-k2-7-code-neuralwatt]
    provider = "neuralwatt"
    upstream_model = "kimi-k2.7-code"

    [models.kimi-k2-7-code-nanogpt]
    provider = "nanogpt"
    upstream_model = "kimi-k2.7-code"

    [models.kimi-k3-kimi-coding]
    provider = "kimi-coding"
    upstream_model = "k3"

    [models.kimi-k3-ocgo]
    provider = "opencode-go"
    upstream_model = "kimi-k3-max"

    [models.kimi-k3-openrouter]
    provider = "openrouter"
    upstream_model = "moonshotai/kimi-k3"

    [models.minimax-m2-5-ocgo]
    provider = "opencode-go-anthropic"
    upstream_model = "minimax-m2.5"
    max_tokens = 131072

    [models.minimax-m2-5-alibaba]
    provider = "alibaba-token-plan"
    upstream_model = "MiniMax-M2.5"

    [models.minimax-m2-7-ocgo]
    provider = "opencode-go-anthropic"
    upstream_model = "minimax-m2.7"
    max_tokens = 131072

    [models.minimax-m2-7-nvidia]
    provider = "nvidia"
    upstream_model = "minimaxai/minimax-m2.7"

    [models.minimax-m3-ocgo]
    provider = "opencode-go"
    upstream_model = "minimax-m3"

    [models.mimo-v2-5-ocgo]
    provider = "opencode-go"
    upstream_model = "mimo-v2.5"

    [models.mimo-v2-5-pro-ocgo]
    provider = "opencode-go"
    upstream_model = "mimo-v2.5-pro"

    [models.mimo-v2-5-pro-xiaomi]
    provider = "xiaomi-mimo"
    upstream_model = "mimo-v2.5-pro"

    # Not `:free`: OpenRouter retired the free tier for this model and now answers
    # that slug with a 404 naming the paid one, which is the same weights (served
    # by Cerebras) at a price this account already pays.
    [models.gpt-oss-120b-openrouter]
    provider = "openrouter"
    upstream_model = "openai/gpt-oss-120b"

    [models.gpt-oss-120b-cerebras]
    provider = "cerebras"
    upstream_model = "gpt-oss-120b"

    [models.nemotron-super-nvidia]
    provider = "nvidia"
    upstream_model = "nvidia/nemotron-3-super-120b-a12b"

    [models.mistral-medium]
    provider = "mistral"
    upstream_model = "mistral-medium-3-5"

    [models.gemma-4-bazaarlink]
    provider = "bazaarlink"
    upstream_model = "gemma-4-26b-a4b-it"

    [models.gemini-flash-lite-puter]
    provider = "puter"
    upstream_model = "google/gemini-3.1-flash-lite-preview"

    [models.gpt-5-6-luna-codex]
    provider = "codex-subscription"
    upstream_model = "gpt-5.6-luna-high"

    [models.gpt-5-6-terra-codex]
    provider = "codex-subscription"
    upstream_model = "gpt-5.6-terra"

    [models.gemini-3-1-pro-antigravity-primary]
    provider = "antigravity-primary"
    upstream_model = "gemini-3.1-pro"
    max_tokens = 65536

    [models.gemini-3-1-pro-antigravity-secondary]
    provider = "antigravity-secondary"
    upstream_model = "gemini-3.1-pro"
    max_tokens = 65536

    [models.gemini-3-1-pro-preview-antigravity-primary]
    provider = "antigravity-primary"
    upstream_model = "gemini-3.1-pro-preview"
    max_tokens = 65536

    [models.gemini-3-1-pro-preview-antigravity-secondary]
    provider = "antigravity-secondary"
    upstream_model = "gemini-3.1-pro-preview"
    max_tokens = 65536

    [models.gemini-3-pro-antigravity-primary]
    provider = "antigravity-primary"
    upstream_model = "gemini-3-pro"
    max_tokens = 65536

    [models.gemini-3-pro-antigravity-secondary]
    provider = "antigravity-secondary"
    upstream_model = "gemini-3-pro"
    max_tokens = 65536

    [models.gemini-3-8-flash-antigravity-primary]
    provider = "antigravity-primary"
    upstream_model = "gemini-3.8-flash-high"
    max_tokens = 65536

    [models.gemini-3-8-flash-antigravity-secondary]
    provider = "antigravity-secondary"
    upstream_model = "gemini-3.8-flash-high"
    max_tokens = 65536

    [models.gemini-3-7-flash-antigravity-primary]
    provider = "antigravity-primary"
    upstream_model = "gemini-3.7-flash-high"
    max_tokens = 65536

    [models.gemini-3-7-flash-antigravity-secondary]
    provider = "antigravity-secondary"
    upstream_model = "gemini-3.7-flash-high"
    max_tokens = 65536

    # Embeddings. 1024 dimensions, which is what poise's photo_analyses.embedding
    # vector column is declared as.
    [models.voyage-3]
    provider = "voyage"
    upstream_model = "voyage-3"

    # ---------------------------------------------------------------------------
    # Combos
    #
    # Strategy is carried over from OmniRoute: `round-robin` spreads first attempts
    # across per-provider free quotas, `priority` prefers a specific target. Under
    # every strategy the remaining targets stay in the list as fallbacks.
    # ---------------------------------------------------------------------------

    [combos.pippin]
    targets = [
      { model = "qwen3-6-35b-yoloauto" },
      { model = "glm-4-5-air-zai" },
    ]

    [combos.glm-5-3]
    targets = [
      { model = "glm-5-3-zai" },
      { model = "glm-5-3-ocgo" },
    ]

    [combos.glm-5-3-flash]
    targets = [
      { model = "glm-5-3-flash-zai" },
      { model = "glm-5-3-flash-pgsgrove" },
    ]

    [combos.glm-5-2]
    targets = [
      { model = "glm-5-2-zai" },
      { model = "glm-5-2-short-neuralwatt" },
      { model = "glm-5-2-alibaba" },
      { model = "glm-5-2-nanogpt" },
    ]

    [combos.glm-5-2-max]
    targets = [
      { model = "glm-5-2-max-zai" },
      { model = "glm-5-2-ocgo" },
      { model = "glm-5-2-alibaba" },
    ]

    [combos.glm-5-1]
    strategy = "round-robin"
    targets = [
      { model = "glm-5-1-zai" },
      { model = "glm-5-1-ocgo" },
    ]

    [combos.glm-5-1-thinking]
    targets = [{ model = "glm-5-1-zai" }]

    [combos.qwen3-7-plus]
    targets = [{ model = "qwen3-7-plus-alibaba" }]

    [combos.qwen3-7-max]
    targets = [
      { model = "qwen3-7-max-alibaba" },
      { model = "qwen3-7-max-ocgo" },
    ]

    [combos.qwen3-8-max]
    targets = [
      { model = "qwen3-8-max-openrouter" },
    ]

    # Vision-focused Google models, distributed across both Antigravity accounts.
    [combos.vision]
    strategy = "round-robin"
    targets = [
      { model = "gemini-3-1-pro-preview-antigravity-primary" },
      { model = "gemini-3-1-pro-preview-antigravity-secondary" },
      { model = "gemini-3-pro-antigravity-primary" },
      { model = "gemini-3-pro-antigravity-secondary" },
    ]

    [combos.qwen3-6-plus]
    strategy = "round-robin"
    targets = [
      { model = "qwen3-6-plus-alibaba" },
      { model = "qwen3-6-plus-ocgo" },
    ]

    [combos.deepseek-v4-flash]
    strategy = "priority"
    targets = [
      { model = "deepseek-v4-flash-pgsgrove" },
      { model = "deepseek-v4-flash-turbo-pgsgrove" },
    ]

    [combos.deepseek-v4-pro]
    targets = [{ model = "deepseek-v4-pro-ocgo" }]

    [combos.kimi-k2-5]
    strategy = "round-robin"
    targets = [
      { model = "kimi-k2-5-alibaba" },
      { model = "kimi-k2-5-ocgo" },
    ]

    [combos.kimi-k2-6]
    targets = [{ model = "kimi-k2-6-ocgo" }]

    [combos.kimi-k2-7-code]
    targets = [
      { model = "kimi-k2-7-code-neuralwatt" },
      { model = "kimi-k2-7-code-ocgo" },
      { model = "kimi-k2-7-code-nanogpt" },
    ]

    [combos.kimi-k3]
    targets = [
      { model = "kimi-k3-openrouter" },
    ]

    [combos.minimax-m2-5]
    strategy = "round-robin"
    targets = [
      { model = "minimax-m2-5-ocgo" },
      { model = "minimax-m2-5-alibaba" },
    ]

    [combos.minimax-m2-7]
    strategy = "round-robin"
    targets = [
      { model = "minimax-m2-7-ocgo" },
      { model = "minimax-m2-7-nvidia" },
    ]

    [combos.minimax-m3]
    targets = [{ model = "minimax-m3-ocgo" }]

    [combos.mimo-v2-5]
    targets = [{ model = "mimo-v2-5-ocgo" }]

    [combos.mimo-v2-5-pro]
    targets = [
      { model = "mimo-v2-5-pro-xiaomi" },
      { model = "mimo-v2-5-pro-ocgo" },
    ]

    # OmniRoute's broad free-tier spread. Round-robin is what keeps any single
    # free quota from carrying the whole load.
    [combos.haldir]
    strategy = "round-robin"
    targets = [
      { model = "qwen3-6-35b-yoloauto" },
      { model = "gpt-oss-120b-cerebras" },
      { model = "gpt-oss-120b-openrouter" },
      { model = "nemotron-super-nvidia" },
      { model = "mistral-medium" },
      { model = "glm-4-5-air-zai" },
      { model = "gemma-4-bazaarlink" },
      { model = "gemini-flash-lite-puter" },
    ]

    [combos.legolas]
    strategy = "round-robin"
    targets = [
      { model = "gpt-oss-120b-openrouter" },
      { model = "gemini-flash-lite-puter" },
    ]

    [combos.gemini-3-1-pro]
    strategy = "round-robin"
    targets = [
      { model = "gemini-3-1-pro-antigravity-primary" },
      { model = "gemini-3-1-pro-antigravity-secondary" },
    ]

    [combos.gemini-3-8-flash]
    strategy = "round-robin"
    targets = [
      { model = "gemini-3-8-flash-antigravity-primary" },
      { model = "gemini-3-8-flash-antigravity-secondary" },
    ]

    [combos.gemini-3-7-flash]
    strategy = "round-robin"
    targets = [
      { model = "gemini-3-7-flash-antigravity-primary" },
      { model = "gemini-3-7-flash-antigravity-secondary" },
    ]

    [combos.radagast]
    strategy = "round-robin"
    targets = [
      { model = "gemini-3-8-flash-antigravity-primary" },
      { model = "gemini-3-8-flash-antigravity-secondary" },
    ]

    [combos.gandalf]
    strategy = "priority"
    targets = [
      { model = "gpt-5-6-luna-codex" },
      { model = "gpt-5-6-terra-codex" },
    ]

    [combos.samwise]
    strategy = "random"
    targets = [
      { model = "glm-5-2-zai" },
      { model = "mimo-v2-5-pro-xiaomi" },
      { model = "qwen3-7-plus-alibaba" },
    ]

    # Image generation for POST /v1/images/generations. Priority, not round-robin:
    # these models differ in price and quality, so the order is the preference and
    # the rest are outage fallbacks. `hidream` is NOT a target — it is the only
    # alias that speaks the NanoGPT `imageDataUrl` edit contract, and an edit
    # request falling through to OpenRouter would come back as an unrelated fresh
    # image rather than an error.
    [combos.image]
    strategy = "priority"
    targets = [
      { model = "gemini-3-1-flash-image-openrouter" },
      { model = "gemini-2-5-flash-image-openrouter" },
      { model = "gpt-5-image-mini-openrouter" },
    ]

    # ---------------------------------------------------------------------------
    # Web search & fetch
    #
    # Pools for POST /v1/search and POST /v1/web/fetch. Independent of the LLM
    # `providers` above: no models, no combos. `pool` is the priority order for a
    # request that does not pin a `provider`; a pinned provider never falls back.
    #
    # Without these tables both pools are empty and Velox answers every web call
    # with 404 web_provider_not_found — it starts fine, so the omission only shows
    # up at request time.
    #
    # SearXNG leads the search pool: it runs in this namespace, needs no key, and
    # already fans out to its own engines (Tavily included), so Tavily is not
    # listed here as a separate Velox provider.
    # ---------------------------------------------------------------------------

    [web.search]
    pool = ["searxng", "exa"]

    # In-cluster Service. The instance must serve `format=json`, which
    # searxng-config.enc.nix enables (search.formats) with the limiter off;
    # otherwise SearXNG answers 403/HTML and the adapter treats it as a failure.
    [web.search.providers.searxng]
    kind = "searxng"
    base_url = "http://searxng:8080"

    [web.search.providers.exa]
    kind = "exa"
    api_key_env = "EXA_API_KEY"

    [web.fetch]
    pool = ["firecrawl", "jina-reader"]

    [web.fetch.providers.firecrawl]
    kind = "firecrawl"
    api_key_env = "FIRECRAWL_API_KEY"

    [web.fetch.providers.jina-reader]
    kind = "jina-reader"
    api_key_env = "JINA_API_KEY"
kind: ConfigMap
metadata:
  annotations:
    kubenix/k8s-version: "1.32"
    kubenix/project-name: ze-homelab
  labels:
    kubenix/hash: 9c253f4a00516b07ba440f9f57e5167913253c13
  name: velox-config
  namespace: apps
---
apiVersion: v1
data:
  agent.toml: |
    # velox-oauth-agent config. Never put secret values here; client_secret_env
    # names an env var injected from velox-config.
    #
    # Credential shapes and the login/rollout procedure:
    # docs/oauth-operator-runbook.md in the velox repo.

    [store]
    secret_name = "velox-oauth-state"
    namespace = "apps"

    [server]
    # Pod probes reach this over the pod IP, so it cannot stay loopback; the
    # metrics surface carries no sensitive data (provider/state labels only).
    bind = "0.0.0.0:8081"

    [runtime]
    dir = "/oauth-runtime"

    [credentials.codex-primary]
    provider = "codex"
    client_id = "app_EMoamEEZ73f0CkXaXp7hrann"
    authorize_url = "https://auth.openai.com/oauth/authorize"
    token_url = "https://auth.openai.com/oauth/token"
    scopes = ["openid", "profile", "email", "offline_access"]
    workspace_id_claim = "https://api.openai.com/auth.chatgpt_account_id"
    # A TOML inline table has to be on one line with commas. Written across
    # several lines it does not parse, the agent refuses to start, and Velox
    # then fails its own startup on the access-token file the agent never wrote.
    extra_authorize_params = { id_token_add_organizations = "true", codex_cli_simplified_flow = "true", originator = "codex_cli_rs", prompt = "login" }

    [credentials.antigravity-primary]
    provider = "antigravity"
    client_id = "1071006060591-tmhssin2h21lcre235vtolojh4g403ep.apps.googleusercontent.com"
    client_secret_env = "ANTIGRAVITY_CLIENT_SECRET"
    authorize_url = "https://accounts.google.com/o/oauth2/v2/auth"
    token_url = "https://oauth2.googleapis.com/token"
    scopes = [
      "https://www.googleapis.com/auth/cloud-platform",
      "https://www.googleapis.com/auth/userinfo.email",
      "https://www.googleapis.com/auth/userinfo.profile",
      "https://www.googleapis.com/auth/cclog",
      "https://www.googleapis.com/auth/experimentsandconfigs"
    ]
    metadata_urls = [
      "https://daily-cloudcode-pa.googleapis.com/v1internal:loadCodeAssist",
      "https://cloudcode-pa.googleapis.com/v1internal:loadCodeAssist",
      "https://daily-cloudcode-pa.sandbox.googleapis.com/v1internal:loadCodeAssist",
    ]
    metadata_body_json = "{\"metadata\":{\"ideType\":\"ANTIGRAVITY\"}}"
    metadata_extract = "cloudaicompanionProject"

    [credentials.antigravity-secondary]
    provider = "antigravity"
    client_id = "1071006060591-tmhssin2h21lcre235vtolojh4g403ep.apps.googleusercontent.com"
    client_secret_env = "ANTIGRAVITY_CLIENT_SECRET"
    authorize_url = "https://accounts.google.com/o/oauth2/v2/auth"
    token_url = "https://oauth2.googleapis.com/token"
    scopes = [
      "https://www.googleapis.com/auth/cloud-platform",
      "https://www.googleapis.com/auth/userinfo.email",
      "https://www.googleapis.com/auth/userinfo.profile",
      "https://www.googleapis.com/auth/cclog",
      "https://www.googleapis.com/auth/experimentsandconfigs"
    ]
    metadata_urls = [
      "https://daily-cloudcode-pa.googleapis.com/v1internal:loadCodeAssist",
      "https://cloudcode-pa.googleapis.com/v1internal:loadCodeAssist",
      "https://daily-cloudcode-pa.sandbox.googleapis.com/v1internal:loadCodeAssist",
    ]
    metadata_body_json = "{\"metadata\":{\"ideType\":\"ANTIGRAVITY\"}}"
    metadata_extract = "cloudaicompanionProject"
kind: ConfigMap
metadata:
  annotations:
    kubenix/k8s-version: "1.32"
    kubenix/project-name: ze-homelab
  labels:
    kubenix/hash: 9c253f4a00516b07ba440f9f57e5167913253c13
  name: velox-oauth-agent-config
  namespace: apps
---
apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
    kubenix/k8s-version: "1.32"
    kubenix/project-name: ze-homelab
  labels:
    kubenix/hash: 9c253f4a00516b07ba440f9f57e5167913253c13
  name: velox-oauth-agent
  namespace: apps
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  annotations:
    kubenix/k8s-version: "1.32"
    kubenix/project-name: ze-homelab
  labels:
    kubenix/hash: 9c253f4a00516b07ba440f9f57e5167913253c13
  name: velox-oauth-agent
  namespace: apps
rules:
  - apiGroups:
      - ""
    resourceNames:
      - velox-oauth-state
    resources:
      - secrets
    verbs:
      - get
      - patch
      - update
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  annotations:
    kubenix/k8s-version: "1.32"
    kubenix/project-name: ze-homelab
  labels:
    kubenix/hash: 9c253f4a00516b07ba440f9f57e5167913253c13
  name: velox-oauth-agent
  namespace: apps
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: velox-oauth-agent
subjects:
  - kind: ServiceAccount
    name: velox-oauth-agent
    namespace: apps
