Task: Raise Synapse resources and add safe cache tuning
File: modules/kubenix/apps/matrix.nix

=== BEFORE ===
resources:
  requests:
    cpu: "100m"
    memory: "256Mi"
  limits:
    cpu: "300m"
    memory: "1Gi"

extraConfig:
  use_presence = false;
  (no cache tuning)

=== AFTER ===
resources:
  requests:
    cpu: "250m"
    memory: "512Mi"
  limits:
    cpu: "1000m"
    memory: "2Gi"

extraConfig:
  use_presence = false;
  caches.global_factor = 1.0;
  event_cache_size = "20K";

=== VERIFICATION ===
make manifests: SUCCESS

=== POST-DEPLOY TIMING CHECK ===
kubectl exec -n apps deploy/synapse-matrix-synapse -c synapse -- curl -s -o /dev/null -w '%{time_total}' http://localhost:8008/_matrix/client/versions

=== NOTES ===
- Synapse uses Recreate strategy (RWO PVC)
- Cache tuning increases memory usage - 2Gi limit accommodates this
- Task 1 baseline: synapse at 473Mi memory, 62m CPU
- Workers NOT enabled (per requirements)
- Existing media_storage_providers config preserved
