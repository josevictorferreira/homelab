# Evidence: Task 6b - Synapse S3 Media Storage Runbook

## Verification Checklist

### File Created
- [x] `.sisyphus/notepads/synapse-s3-media-rgw/runbook.md` exists

### Sections Present
- [x] Overview with key configuration
- [x] Outage Response (RGW/S3 Down)
- [x] Weekly Maintenance (Sync + Prune)
- [x] Rollback Procedure
- [x] Known Limitations and Risks
- [x] Quick Reference Commands

### Concrete Commands Verified

#### Diagnostic Commands
```bash
# S3 connectivity check
kubectl exec -n apps deploy/synapse-matrix-synapse -c synapse -- python -c 'import boto3; s3 = boto3.client("s3", endpoint_url="http://rook-ceph-rgw-ceph-objectstore.rook-ceph.svc.cluster.local"); print(s3.list_buckets())'

# RGW pod status
kubectl get pods -n rook-ceph -l app=rook-ceph-rgw

# Provider logs
kubectl logs -n apps deploy/synapse-matrix-synapse -c synapse | grep -iE "s3|boto|upload|storage" | tail -50

# Local media store usage
kubectl exec -n apps deploy/synapse-matrix-synapse -c synapse -- du -sh /data/media_store
```

#### Weekly Sync Commands
```bash
# Temp pod creation with correct image and volumes
kubectl run synapse-media-sync --rm -i --restart=Never --namespace apps --image ghcr.io/element-hq/synapse:v1.146.0 --overrides='...'

# Provider install
pip install --no-cache-dir --target /modules synapse-s3-storage-provider

# Update records
python -m s3_storage_provider.s3_media_upload update /data/media_store 30d

# Upload to S3
python -m s3_storage_provider.s3_media_upload upload /data/media_store matrix-synapse-media

# Upload with delete
python -m s3_storage_provider.s3_media_upload upload /data/media_store matrix-synapse-media --delete

# Prune old files
find /data/media_store -type f -mtime +30 -delete
```

#### Verification Commands
```bash
# Check OBC secret
kubectl get secret -n apps matrix-synapse-media

# Verify S3 objects
python -c "import boto3; s3 = boto3.client('s3', endpoint_url='http://rook-ceph-rgw-ceph-objectstore.rook-ceph.svc.cluster.local'); response = s3.list_objects_v2(Bucket='matrix-synapse-media', Prefix='synapse/', MaxKeys=10); print(f\"Found {response.get('KeyCount', 0)} objects\")"
```

### Configuration References Verified
- Bucket name: `matrix-synapse-media` (from matrix.nix line 13)
- Endpoint: `kubenix.lib.objectStoreEndpoint` resolves to `http://rook-ceph-rgw-ceph-objectstore.rook-ceph.svc.cluster.local`
- Media path: `/data/media_store` (from learnings.md line 11)
- Namespace: `apps` (from matrix.nix line 6)
- Synapse image: `ghcr.io/element-hq/synapse:v1.146.0` (from matrix.nix line 31)

### No TODO Placeholders
All commands are concrete and executable. No placeholder values like "REPLACE_ME" or "CHANGEME" present.

## Status: COMPLETE
